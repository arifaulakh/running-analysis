# Semantic memory

Curated claims about Arif. Written only by the `memory-observer` subagent.
Read by the running-coach skill on every invocation.

Format: one frontmatter block per claim, separated by blank lines. See
`.claude/skills/running-coach/reference/memory_protocol.md` for rules.

A claim is **active** if `superseded_by: null`. The Coach reads all
active claims.

<!-- Claims will be appended below as the Observer promotes them. -->
